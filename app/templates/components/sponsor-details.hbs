{{#with @sponsor as |sponsor|}}
  <a class="sponsor-details-link" target="_blank" href="{{sponsor.website}}">
    <img class="sponsor-details-logo" alt="{{sponsor.sponsorText}}" src="{{sponsor.imageUrl}}">
  </a>
{{/with}}
